export declare type VerifierOptions = {
    data: Uint8Array;
    signature: Uint8Array;
};
export declare type Verifier = {
    verify: (options: VerifierOptions) => Promise<boolean>;
};
export declare type JwsVerifierOptions = {
    signature: string;
};
export declare type JwsVerifier = {
    verify: (options: JwsVerifierOptions) => Promise<boolean>;
};
export declare type DetachedJwsVerifierOptions = {
    data: Uint8Array;
    signature: string;
};
export declare type DetachedJwsVerifier = {
    verify: (options: DetachedJwsVerifierOptions) => Promise<boolean>;
};
