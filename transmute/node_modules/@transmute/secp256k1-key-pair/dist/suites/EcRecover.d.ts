export declare const signer: (privateKey: Uint8Array) => {
    sign({ data }: any): Promise<Uint8Array>;
};
export declare const verifier: (publicKey: Uint8Array) => {
    verify({ data, signature }: any): Promise<boolean>;
};
