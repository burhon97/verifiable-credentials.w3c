import { JsonWebKey2020, Ed25519VerificationKey2018 } from './types';
import { LdKeyPairInstance } from '@transmute/ld-key-pair';
import { X25519KeyPair } from '@transmute/x25519-key-pair';
export declare class Ed25519KeyPair implements LdKeyPairInstance {
    id: string;
    type: string;
    controller: string;
    publicKey: Uint8Array;
    privateKey?: Uint8Array;
    static fingerprintFromPublicKey(importableType: JsonWebKey2020): Promise<string>;
    static toX25519KeyPair(kp: Ed25519KeyPair): Promise<X25519KeyPair>;
    static generate: ({ secureRandom, }: {
        secureRandom: () => Uint8Array;
    }) => Promise<Ed25519KeyPair>;
    static from: (k: JsonWebKey2020 | Ed25519VerificationKey2018) => Promise<Ed25519KeyPair>;
    static fromFingerprint({ fingerprint }: {
        fingerprint: string;
    }): Promise<Ed25519KeyPair>;
    constructor(opts: {
        id: string;
        type: string;
        controller: string;
        publicKey: Uint8Array;
        privateKey?: Uint8Array;
    });
    fingerprint(): Promise<string>;
    export(options?: {
        privateKey?: boolean;
        type: 'JsonWebKey2020' | 'Ed25519VerificationKey2018';
    }): Promise<JsonWebKey2020 | Ed25519VerificationKey2018>;
    signer(type?: 'EdDsa'): {
        sign({ data }: any): Promise<Uint8Array>;
    };
    verifier(type?: 'EdDsa'): {
        verify({ data, signature }: any): Promise<boolean>;
    };
    getDerivedKeyPairs(): Promise<(this | X25519KeyPair)[]>;
}
