import { ControllerProofPurpose } from "./ControllerProofPurpose";
import { IPurposeValidateOptions } from "../types";
export declare class AuthenticationProofPurpose extends ControllerProofPurpose {
    challenge: string;
    domain: string;
    constructor({ term, controller, challenge, date, domain, maxTimestampDelta }?: any);
    validate(proof: any, { verificationMethod, documentLoader, expansionMap }: IPurposeValidateOptions): Promise<any>;
    update(proof: any, _options: IPurposeValidateOptions): Promise<any>;
}
