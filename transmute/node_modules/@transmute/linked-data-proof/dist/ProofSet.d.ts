import { IProofSetAddOptions } from "./types";
export declare class ProofSet {
    add(document: any, { suite, purpose, documentLoader, expansionMap, compactProof }?: IProofSetAddOptions): Promise<any>;
    private _getProofs;
    private _verify;
    private _addToJSON;
    verify: (document: any, { suite, purpose, documentLoader, expansionMap, compactProof }?: any) => Promise<any>;
}
