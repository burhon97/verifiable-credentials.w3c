import { LdKeyPairStatic } from '@transmute/ld-key-pair';
export declare const getGenerator: (KeyPairClass: LdKeyPairStatic) => (keyGenOptions: any, resolutionOptions?: any) => Promise<{
    didDocument: import("./types").DidDocument;
    keys: any[];
}>;
