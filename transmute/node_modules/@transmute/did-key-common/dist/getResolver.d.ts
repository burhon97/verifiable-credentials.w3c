import { ResolutionOptions, ResolutionResponse } from './types';
import { LdKeyPairStatic } from '@transmute/ld-key-pair';
export declare const getResolver: (KeyPairClass: LdKeyPairStatic) => (did: string, options?: ResolutionOptions) => Promise<ResolutionResponse>;
