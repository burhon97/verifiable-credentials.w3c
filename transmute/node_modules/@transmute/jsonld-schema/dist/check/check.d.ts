import CheckResult from './CheckResult';
declare const check: ({ input, schema, documentLoader, }: {
    input: string | object;
    schema?: any;
    documentLoader: (iri: string) => {
        document: any;
    };
}) => Promise<CheckResult>;
export default check;
