export declare class EncryptTransformer {
    alg: string;
    enc: string;
    recipients: any;
    chunkSize: any;
    offset: any;
    totalOffset: any;
    index: any;
    buffer: any;
    constructor({ alg, enc, recipients, chunkSize, }?: any);
    start(): void;
    transform(chunk: any, controller: any): Promise<void>;
    flush(controller: any): Promise<void>;
    encrypt(data: Uint8Array): Promise<any>;
}
