import { Verifier, JWA_ALG } from '../types';
export declare const createVerifier: (verifier: Verifier, type: JWA_ALG, options?: any) => {
    verify: ({ data, signature, }: {
        data?: Uint8Array | undefined;
        signature: string;
    }) => Promise<boolean>;
};
